import { StyleSheet, Text, View } from "react-native";

const power = () => {
  return (
    <View>
      <Text>power</Text>
    </View>
  );
};

export default power;

const styles = StyleSheet.create({});
