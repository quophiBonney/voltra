import { StyleSheet, Text, View } from "react-native";

const docs = () => {
  return (
    <View>
      <Text>docs</Text>
    </View>
  );
};

export default docs;
const styles = StyleSheet.create({});
